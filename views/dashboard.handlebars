<section class="block">
    {{#if journals}}
    <div class="columns is-centered my-4">
        <div class="column is-two-thirds-tablet is-three-quarters-desktop my-3">
            <h2 class="p-3 is-size-3  has-text-black has-border-color-black is-rounded">Select a Journal</h2>
        </div>
        <div class="column is-one-third-tablet is-one-quarter-desktop my-3">
            <button id="create-journal" class="button is-black is-outlined">
                <span class="is-hidden-tablet">
                    <i class="fa-solid fa-plus is-size-3 is-black"></i>
                </span>
                <span class="is-hidden-mobile is-size-3 has-text-black">Create New Journal</span>
            </button>
        </div>
    </div>

    <div class="columns is-centered my-3">
        {{#each journals}}
        {{>journal-partial}}
        {{/each}}
    </div>

    {{else}}
    <div class="hero is-halfheight">
        <div class="hero-body justify-content-center">
            <button id="create-journal" class="button is-black is-outlined">
                <span class="is-hidden-mobile is-size-3 has-text-black">Create New Journal</span>
            </button>
        </div>
    </div>
    {{/if}}
</section>

<script src="/js/createJournalButton.js"></script>
{{#if journals}}
<script src="/js/journal.js"></script>
{{/if}}