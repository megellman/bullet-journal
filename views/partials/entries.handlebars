<div class="columns">
    <div class="column">
        <div class="box {{background}}">
            <a href="journals/{{id}}/entries/{{id}}">
                <p class="title">
                    {{title}}
                    <span class="icon is-small"> 
                        <i data-id="{{id}}" class="fa-solid fa-pencil"></i>
                        <i data-id="{{id}}" class="fa-solid fa-trash-can"></i>
                    </span>
                </p>
            </a>
        </div>
    </div>
</div>
<div>
    
</div>